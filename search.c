/*
void input(int[], int);
void output(int[], int);
void search(int[], int);

int main()
{
    int a[100], n;
    printf("enter the number of elements:\n");
    scanf("%d", &n);

    input(a, n);
    output(a, n);

    return 0;
}
void input(int a[], int n)
{

    for (int i = 0; i < n; i++)
    {
        printf("enter the elements\n");
        scanf("%d", &a[i]);
    }
}
void search(int c[], int s)
{
    for (int i = 0; i < n; i++)
    {
    }
}
void output(int b[], int m)
{

    for (int i = 0; i < m; i++)
    {
        if (a[i] =)
            printf("%d", b[i]);
    }
}
*/
/*
#include <stdio.h>
int main()
{
    int arr[] = {2, 4, 5, 6, 778, 65, 79, 23, 1};
    int n = (sizeof(arr) / sizeof(arr[0]));
    int even_count = 0, odd_count = 0;
    for (int i = 0; i < n; i++)
    {
        if ((arr[i] & 1) == 0)
            even_count++;
        else
            odd_count++;
    }
    printf("the number of even elements is %d\n the count of odd elements is %d", even_count, odd_count);
    return 0;
}
*/